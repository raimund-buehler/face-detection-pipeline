Non-overlapping time-bin random-structure sensitivity check

Input:
duration_sliding_window_data.csv, restricted to 10%-wide windows starting at 0%, 10%, ..., 90%

Fixed-effects part:
window_fix_duration_prop_beta ~ Group * progress_bin * medication + Sex + session

Random structures compared:
id_intercept: (1 | ID)
id_plus_subject_session: (1 | ID) + (1 | subject_session)
id_plus_medication_diag: (1 | ID) + diag(0 + medication | ID)
id_plus_session_diag: (1 | ID) + diag(0 + session | ID)
id_plus_subject_session_plus_medication_diag: (1 | ID) + (1 | subject_session) + diag(0 + medication | ID)

Interpretation note:
This is a sensitivity check for whether medication/session effects and Group x medication effects depend on random-structure assumptions.

Study-design note:
Medication was administered within subject, with one medication condition per interview session and randomized session order. Therefore, time-bin models must account for bins nested within participant-session and/or medication-within-participant dependency. See docs/study_design_modeling_notes.md.
