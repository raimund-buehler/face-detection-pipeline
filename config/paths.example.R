# Copy to config/paths.R and set for your machine. config/paths.R is gitignored.
#
# recordings_root : directory holding the per-session eye-tracking exports
# id_corrections  : any ID typos to repair on read, as c(wrong = "right")

recordings_root <- "/path/to/recordings_sorted"
id_corrections  <- c()
